# Core environment (Python 3.9.7)
pandas==1.3.4
numpy==1.20.3
scikit-learn
matplotlib

# For exact reproducibility, regenerate from the analysis environment:
#   pip freeze > requirements.txt
# Note: statsmodels and pyarrow were intentionally not used in this environment.
